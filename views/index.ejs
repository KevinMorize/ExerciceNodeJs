<%- include('./includes/headerIndex.ejs')%>

  <form class="ui form" action="/login" method="post">

    <% if (locals.confirmMessage) { %>
      <div class="ui green message"><%- confirmMessage %></div>
    <% } %>

    <% if (locals.message) { %>
      <div class="ui red message"><%- message %></div>
    <% } %>

    <div class="field">
      <input type="email" placeholder="E-mail" name="email" >
    </div> 

    <div class="field">
      <input type="password" placeholder="Mot de passe" name="password" ">
    </div>
 
    <div class="loginDivButton">
      <button class="login-button" type="submit">Connexion</button>
      <img src="/img/balades.png" alt="img-login">
    </div>

    <a href="" id="">Mot de passe oublié?</a>

    <p>Pas encore inscrit ? :</p>

    <div class="loginDivButton">
      <img src="/img/balades.png" alt="img-login">
      <a href="/inscription" class="login-button">Inscription </a>
    </div>

  </form>

<%- include ('./includes/footerIndex.ejs') %>